<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jh.albaro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".FindPwd.FindPWActivity" />
        <activity android:name=".FindId.FindIDActivity" />
        <activity android:name=".FindPwd.SettingPWActivity" />
        <activity android:name=".FindPwd.AuthEmailActivity" />
        <activity android:name=".Register.RegisterNextActivity" />
        <activity android:name=".Register.RegisterFinishActivity" />
        <activity android:name=".StoreListActivity" />
        <activity android:name=".WorkSpace.WorkspaceActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".WorkSpace.AddworkActivity" />
        <activity android:name=".WorkSpace.Workspace_Dialog"></activity>
    </application>

</manifest>